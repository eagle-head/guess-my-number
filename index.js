/**
 * @format
 */

import { AppRegistry } from "react-native";
import App from "./src/App";
import { name as guessMyNumber } from "./app.json";

AppRegistry.registerComponent(guessMyNumber, () => App);
